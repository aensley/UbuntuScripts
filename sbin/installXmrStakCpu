#!/usr/bin/env bash

XMR_CONFIG_DIR='/opt/xmr-config/'
XMR_CONFIG_FILE="${XMR_CONFIG_DIR}xmr-stak-cpu-config.txt"
XMR_STAK_DIR='/opt/xmr-stak-cpu/'
XMR_STAK_BIN="${XMR_STAK_DIR}bin/xmr-stak-cpu"
XMR_LOG_FILE='/var/log/xmrStakCpu.log'
XMR_LOG_FILE_ESCAPED='\/var\/log\/xmrStakCpu.log'

expectedXmrCpuHashRate() {

  # forked from: https://moneroocean.stream/miner/setup_moneroocean_miner.sh

  # checking prerequisites

  if ! which lscpu >/dev/null; then
    echo "ERROR: This script requires \"lscpu\" utility to work correctly"
    exit 1
  fi

  # calculating port

  LSCPU=`lscpu`
  CPU_SOCKETS=`echo "$LSCPU" | grep "^Socket(s):" | cut -d':' -f2 | sed "s/^[ \t]*//"`
  if [ -z $CPU_SOCKETS ]; then
    echo "ERROR: Can't get CPU sockets from lscpu output"
    exit 1
  fi
  CPU_CORES_PER_SOCKET=`echo "$LSCPU" | grep "^Core(s) per socket:" | cut -d':' -f2 | sed "s/^[ \t]*//"`
  if [ -z $CPU_CORES_PER_SOCKET ]; then
    echo "ERROR: Can't get CPU cores per socket from lscpu output"
    exit 1
  fi
  CPU_THREADS=`echo "$LSCPU" | grep "^CPU(s):" | cut -d':' -f2 | sed "s/^[ \t]*//"`
  if [ -z $CPU_THREADS ]; then
    echo "ERROR: Can't get CPU cores from lscpu output"
    exit 1
  fi
  CPU_MHZ=`echo "$LSCPU" | grep "^CPU MHz:" | cut -d':' -f2 | sed "s/^[ \t]*//"`
  CPU_MHZ=${CPU_MHZ%.*}
  if [ -z $CPU_MHZ ]; then
    echo "ERROR: Can't get CPU MHz from lscpu output"
    exit 1
  fi
  CPU_L1_CACHE=`echo "$LSCPU" | grep "^L1d cache:" | cut -d':' -f2 | sed "s/^[ \t]*//" | sed "s/K\$//"`
  if [ -z $CPU_L1_CACHE ]; then
    echo "ERROR: Can't get L1 CPU cache from lscpu output"
    exit 1
  fi
  CPU_L2_CACHE=`echo "$LSCPU" | grep "^L2 cache:" | cut -d':' -f2 | sed "s/^[ \t]*//" | sed "s/K\$//"`
  if [ -z $CPU_L2_CACHE ]; then
    echo "ERROR: Can't get L2 CPU cache from lscpu output"
    exit 1
  fi
  CPU_L3_CACHE=`echo "$LSCPU" | grep "^L3 cache:" | cut -d':' -f2 | sed "s/^[ \t]*//" | sed "s/K\$//"`
  if [ -z $CPU_L3_CACHE ]; then
    echo "ERROR: Can't get L3 CPU cache from lscpu output"
    exit 1
  fi

  TOTAL_CACHE=$(( $CPU_THREADS*$CPU_L1_CACHE + $CPU_SOCKETS * ($CPU_CORES_PER_SOCKET*$CPU_L2_CACHE + $CPU_L3_CACHE)))
  if [ -z $TOTAL_CACHE ]; then
    echo "ERROR: Can't compute total cache"
    exit 1
  fi
  EXP_MONERO_HASHRATE=$(( ($CPU_THREADS < $TOTAL_CACHE / 2048 ? $CPU_THREADS : $TOTAL_CACHE / 2048) * ($CPU_MHZ * 20 / 1000) ))
  if [ -z $EXP_MONERO_HASHRATE ]; then
    echo "ERROR: Can't compute projected Monero hashrate"
    exit 1
  fi

  echo "CPU Threads:        $CPU_THREADS"
  echo "CPU Speed:          $CPU_MHZ MHz"
  echo "CPU Cache:          ${TOTAL_CACHE}KB"
  echo "Projected hashrate: ~$EXP_MONERO_HASHRATE H/s"

}

createDonateLevelH() {
  cat >/tmp/donate-level.h <<EOL
#pragma once

constexpr double fDevDonationLevel = 0.0;
EOL
}

createService() {
  cat >/tmp/xmr-stak-cpu.service <<EOL
[Unit]
Description=XMR Stak CPU Mining service

[Service]
ExecStart=${XMR_STAK_BIN} ${XMR_CONFIG_FILE}
Nice=10

[Install]
WantedBy=multi-user.target
EOL
}

createLogRotateFile() {
  cat >/tmp/xmrStakCpu <<EOL
${XMR_LOG_FILE} {
        weekly
        rotate 4
        missingok
        create 644 root root
        compress
        notifempty
}
EOL
}

if [ "$1" = "--uninstall" ]; then

  # Remove xmr-stak-cpu
  echoheader "Uninstalling xmr-stak-cpu"
  sudo systemctl stop xmr-stak-cpu.service
  sudo systemctl disable xmr-stak-cpu.service
  sudo rm -f /etc/systemd/system/xmr-stak-cpu.service
  sudo systemctl daemon-reload
  sudo rm -f /etc/logrotate.d/xmrStakCpu
  sudo rm -f ${XMR_LOG_FILE}
  sudo rm -rf ${XMR_STAK_DIR}
  sudo rm -f ${XMR_CONFIG_FILE}
  sudo sed -i 's/vm.nr_hugepages=128//' /etc/sysctl.conf
  sudo sed -i 's/* soft memlock 262144//' /etc/security/limits.conf
  sudo sed -i 's/* hard memlock 262144//' /etc/security/limits.conf
  echoheader "Finished!"

else

  if [ -d "${XMR_STAK_DIR}" ]; then

    # Upgrade xmr-stak-cpu
    echoheader "Upgrading xmr-stak-cpu"
    sudo systemctl stop xmr-stak-cpu.service
    cd ${XMR_STAK_DIR} && \
    sudo git clean -fdx && \
    sudo git pull && \
    createDonateLevelH && \
    sudo mv -f /tmp/donate-level.h ${XMR_STAK_DIR}donate-level.h && \
    sudo cmake -DMICROHTTPD_ENABLE=OFF -DOpenSSL_ENABLE=OFF -DHWLOC_ENABLE=ON . && \
    sudo make && \
    sudo systemctl start xmr-stak-cpu.service
    echoheader "Finished!"

  else

    # Install xmr-stak-cpu
    echoheader "Installing xmr-stak-cpu"
    expectedXmrCpuHashRate
    echoheader

    ## Build
    sudo apt -y install git libmicrohttpd-dev libssl-dev cmake build-essential libhwloc-dev && \
    sudo mkdir -p ${XMR_CONFIG_DIR} && \
    sudo git clone https://github.com/fireice-uk/xmr-stak-cpu.git ${XMR_STAK_DIR} && \
    createDonateLevelH && \
    sudo mv -f /tmp/donate-level.h ${XMR_STAK_DIR}donate-level.h && \
    sudo chown root:root ${XMR_STAK_DIR}donate-level.h && \
    cd ${XMR_STAK_DIR} && \
    sudo cmake -DMICROHTTPD_ENABLE=OFF -DOpenSSL_ENABLE=OFF -DHWLOC_ENABLE=ON . && \
    sudo make

    ## System Configuration
    sudo sysctl -w vm.nr_hugepages=128
    echo "vm.nr_hugepages=128" | sudo tee -a /etc/sysctl.conf
    echo "* soft memlock 262144" | sudo tee -a /etc/security/limits.conf
    echo "* hard memlock 262144" | sudo tee -a /etc/security/limits.conf

    ## Service
    createService && \
    sudo mv /tmp/xmr-stak-cpu.service /etc/systemd/system/xmr-stak-cpu.service
    sudo chown root:root /etc/systemd/system/xmr-stak-cpu.service && \
    sudo systemctl daemon-reload
    sudo systemctl enable xmr-stak-cpu.service

    ## Log file
    sudo touch ${XMR_LOG_FILE}
    createLogRotateFile && \
    sudo mv /tmp/xmrStakCpu /etc/logrotate.d/xmrStakCpu
    sudo chown root:root /etc/logrotate.d/xmrStakCpu && \

    ## xmr-stak-cpu configuration
    sudo cp ${XMR_STAK_DIR}config.txt ${XMR_CONFIG_FILE}
    BEST_CONF=`sudo ${XMR_STAK_BIN} ${XMR_CONFIG_FILE} | sed -n "/BEGIN/, /END/ p" | grep -v '\*'`

    WALLET="$1"
    while [ -z "$WALLET" ]; do
      echo
      echo -n "Enter wallet address: "
      read -e WALLET
    done

    PASSWORD="$2"
    while [ -z "$PASSWORD" ]; do
      echo
      echo -n "Enter password (MinerID:EmailAddress): "
      read -e PASSWORD
    done

    sudo perl -i -0pe "s/\"cpu_threads_conf\" :\s+null,/$BEST_CONF/g" ${XMR_CONFIG_FILE}
    sudo sed -i 's/"use_slow_memory" : *"[^"]*",/"use_slow_memory" : "no_mlck",/' ${XMR_CONFIG_FILE}
    sudo sed -i 's/"verbose_level" : 3,/"verbose_level" : 4,/' ${XMR_CONFIG_FILE}
    sudo sed -i 's/"output_file" : "",/"output_file" : "'${XMR_LOG_FILE_ESCAPED}'",/' ${XMR_CONFIG_FILE}
    sudo sed -i 's/"pool_address" : *"[^"]*",/"pool_address" : "gulf.moneroocean.stream:10001",/' ${XMR_CONFIG_FILE}
    sudo sed -i 's/"wallet_address" : *"[^"]*",/"wallet_address" : "'${WALLET}'",/' ${XMR_CONFIG_FILE}
    sudo sed -i 's/"pool_password" : *"[^"]*",/"pool_password" : "'${PASSWORD}'",/' ${XMR_CONFIG_FILE}

    echo
    echo "This is the best configuration xmr-stak-cpu"
    echo "has automatically detected for this system:"
    echo "$BEST_CONF"
    echo
    echo "If you need to change this, be sure to edit the config file:"
    echo "    sudo nano ${XMR_CONFIG_FILE}"
    echo
    echo "You should also check the port used in \"pool_address\" the same way."
    echo "    Available ports: https://moneroocean.stream/#/ports"
    echo
    echoheader "Finished!"

  fi
fi
